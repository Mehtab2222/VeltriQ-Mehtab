@model VeltriQ.Models.HR.Division

@{
    ViewData["Title"] = "Create Division";
}

<style>
    /* ── ULTRA-THIN GLOBAL SCROLLBAR FORCE OVERRIDES ── */
    *::-webkit-scrollbar {
        width: 4px !important;
        height: 4px !important;
    }

    *::-webkit-scrollbar-track {
        background: transparent !important;
    }

    *::-webkit-scrollbar-thumb {
        background: var(--border) !important;
        border-radius: 10px !important;
    }

        *::-webkit-scrollbar-thumb:hover {
            background: var(--purple) !important;
        }

    * {
        scrollbar-width: thin !important;
        scrollbar-color: var(--border) transparent !important;
    }

    .division-create-page {
        width: 100%;
        display: flex;
        flex-direction: column;
        padding-top: 16px;
        padding-bottom: 30px;
    }

    /* TOPBAR */
    .division-topbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 14px;
        gap: 16px;
    }

    .division-breadcrumb {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 12.5px;
    }

        .division-breadcrumb a {
            color: var(--purple);
            text-decoration: none;
            font-weight: 600;
        }

        .division-breadcrumb i {
            color: var(--text-muted);
            font-size: 10px;
        }

        .division-breadcrumb span {
            color: var(--text-secondary);
            font-weight: 500;
        }

    .division-top-actions {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .cancel-btn {
        height: 32px;
        padding: 0 14px;
        border-radius: 4px;
        border: 1px solid var(--border);
        background: var(--hover-bg);
        color: var(--text-primary);
        font-size: 11.5px;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        text-decoration: none;
        transition: all 0.2s ease;
    }

        .cancel-btn:hover {
            background: var(--purple);
            color: #fff;
            border-color: var(--purple);
        }

    .save-btn {
        height: 32px;
        padding: 0 16px;
        border: none;
        border-radius: 4px;
        background: var(--purple);
        color: #fff;
        font-size: 11.5px;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 6px;
        cursor: pointer;
        transition: all 0.2s ease;
    }

        .save-btn:hover {
            opacity: 0.9;
        }

    /* SINGLE COMPACT CARD MODULE (3 Columns per row) */
    .division-master-card {
        background: var(--card-bg);
        border: 1px solid var(--border);
        border-radius: 0px;
        display: flex;
        flex-direction: column;
        overflow: hidden;
        padding: 20px;
    }

    .division-card-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;
    }

    .col-span-2 {
        grid-column: span 2;
    }

    .col-span-3 {
        grid-column: span 3;
    }

    /* FIELD */
    .form-field {
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

        .form-field label {
            font-size: 10px;
            font-weight: 700;
            letter-spacing: 0.5px;
            color: var(--text-secondary);
            text-transform: uppercase;
        }

    /* INPUT */
    .form-input {
        width: 100%;
        height: 34px;
        border-radius: 4px;
        border: 1px solid var(--border);
        background: var(--input-bg);
        color: var(--text-primary);
        padding: 0 10px;
        font-size: 12px;
        outline: none;
        transition: border-color 0.2s ease;
    }

        .form-input:focus {
            border-color: var(--purple);
        }

        .form-input::placeholder {
            color: var(--text-muted);
        }

    /* RESPONSIVE */
    @@media(max-width: 1024px) {
        .division-card-grid {
            grid-template-columns: repeat(2, 1fr);
        }

        .col-span-2, .col-span-3 {
            grid-column: span 2;
        }
    }

    @@media(max-width: 640px) {
        .division-card-grid {
            grid-template-columns: 1fr;
        }

        .col-span-2, .col-span-3 {
            grid-column: span 1;
        }

        .division-topbar {
            flex-direction: column;
            align-items: flex-start;
        }

        .division-top-actions {
            width: 100%;
        }

        .cancel-btn,
        .save-btn {
            flex: 1;
            justify-content: center;
        }
    }
</style>

<div class="division-create-page">

    <div class="division-topbar">
        <div class="division-breadcrumb">
            <a href="/Division/Index">Divisions</a>
            <i class="bi bi-chevron-right"></i>
            <span>Create</span>
        </div>

        <div class="division-top-actions">
            <a href="/Division/Index" class="cancel-btn">
                <i class="bi bi-x"></i> Cancel
            </a>

            <button type="submit" form="divisionForm" class="save-btn">
                <i class="bi bi-diagram-3"></i> Save Division
            </button>
        </div>
    </div>

    <form id="divisionForm" asp-action="Create" method="post">
        @Html.AntiForgeryToken()

        <div class="division-master-card">
            <div class="division-card-grid">

                <div class="form-field">
                    <label>Division Name *</label>
                    <input asp-for="DivisionName" class="form-input" placeholder="Enter division name" autocomplete="off" />
                </div>

                <div class="form-field">
                    <label>Division Code *</label>
                    <input asp-for="DivisionCode" class="form-input" placeholder="DIV-001" autocomplete="off" />
                </div>

                <div class="form-field">
                    <label>Status</label>
                    <input value="Active" class="form-input" readonly />
                </div>

                <div class="form-field col-span-2">
                    <label>Description</label>
                    <input asp-for="Description" class="form-input" placeholder="Enter division description" autocomplete="off" />
                </div>

            </div>
        </div>
    </form>
</div>